server:
  port: 8888
spring:
  profiles:
    active: git
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/devjclopez/config-server-bootcamp
          default-label: main

